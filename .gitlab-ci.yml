stages:
  - provisioning

creating-infraestructure-do:
  image: hashicorp/terraform:1.3.0
  stage: provisioning
  script:
    - terraform init -chdir="./modules/k8s-digital-ocean" -backend-config="conn_str=${TF_PG_CONN}" -backend-config="schema_name=infra1"
  rules:
    - when: manual